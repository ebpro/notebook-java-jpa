CREATE TABLE LOCATION (ID VARCHAR NOT NULL, DESCRIPTION VARCHAR, PRIMARY KEY (ID))
CREATE TABLE SENSOR (ID BIGINT NOT NULL, TYPE INTEGER, LOCATION VARCHAR, PRIMARY KEY (ID))
ALTER TABLE SENSOR ADD CONSTRAINT FK_SENSOR_LOCATION FOREIGN KEY (LOCATION) REFERENCES LOCATION (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
